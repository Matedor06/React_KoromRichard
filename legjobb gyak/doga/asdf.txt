npm install eslint --save-dev
npm run lint

npm install vitest

"scripts": {
    "test": "vitest",
    "lint": "eslint .",
    // ...
}


npm i happy-dom @testing-library/react



// @vitest-environment happy-dom

import { render, screen } from '@testing-library/react';
import { describe, it, expect } from 'vitest';
import App from './App';

describe('App (real API)', () => {
    it('igazi API-ból betölti és megjeleníti a Margheritát', async () => {
        render(<App />);

        // megvárja, amíg a useEffect lefut + API válasz megjön + render
        const el = await screen.findByText('Margherita', {}, { timeout: 10000 });
        expect(el).toBeTruthy();
    });
});




// @vitest-environment happy-dom

import { render, screen } from '@testing-library/react';
import { it } from 'vitest';
import { MemoryRouter } from 'react-router-dom';
import AllPizza from '../pages/AllPizza';

it('igazi API-ből betölti a Margheritát', async () => {
    render(<MemoryRouter><AllPizza /></MemoryRouter>);
    await screen.findByText('Margherita', {}, { timeout: 10000 });
});

npm run test
